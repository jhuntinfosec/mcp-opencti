# Testing dependencies for OpenCTI MCP Server
# Install with: pip install -r requirements-test.txt

# Core testing framework
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Code quality and linting
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0

# Production dependencies (should match main requirements)
mcp[fastmcp]>=0.1.0
pycti>=6.0.0
